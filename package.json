{
  "name": "rafed-hack",
  "version": "1.0.0",
  "private": true,
  "description": "Rafed Hack - Client and Server",
  "scripts": {
    "dev": "pnpm run dev:server & pnpm run dev:client",
    "dev:client": "cd client && pnpm dev",
    "dev:server": "cd server && pnpm run dev",
    "install:all": "pnpm install && cd client && pnpm install && cd ../server && pnpm install"
  },
  "devDependencies": {}
}
